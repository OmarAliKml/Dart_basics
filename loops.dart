void main() {
  // Initialize a list with mixed types: int, double, boolean, and string
  var x = [3, 5.6, true, 'Ali'];

  // Iterate over each element in the list using a for-in loop
  for (var i in x) {
    print(i); // Print each element
  }
}
